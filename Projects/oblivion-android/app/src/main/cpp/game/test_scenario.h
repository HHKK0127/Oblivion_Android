#pragma once

class Renderer;

/**
 * Create a test scenario with sample NPCs, quests, spells, and combat.
 * This is a temporary development helper and should be replaced by
 * proper game initialization in production.
 */
void createTestScenario(Renderer* renderer);